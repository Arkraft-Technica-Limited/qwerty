# .gitattributes for Android/Gradle/Kotlin Projects
# Prevents line-ending issues across Windows, macOS, and Linux.

# 1. Default behavior:
# Handle all files as text and normalize them (LF in repo) unless specified otherwise.
* text=auto

# 2. Source & Config Files:
# Force LF for all source code, resources, and configuration files.
# This ensures consistency across different OSs and IDEs.
*.kt           text eol=lf
*.kts          text eol=lf
*.java         text eol=lf
*.xml          text eol=lf
*.gradle       text eol=lf
*.properties   text eol=lf
*.pro          text eol=lf
*.txt          text eol=lf
*.md           text eol=lf
*.json         text eol=lf
*.yml          text eol=lf
*.yaml         text eol=lf

# 3. Executables & Scripts:
# Force LF for shell scripts to ensure they work in Linux, WSL, and GitHub Actions.
*.sh           text eol=lf
gradlew        text eol=lf

# Force CRLF for Windows batch files so they remain compatible with Command Prompt.
*.bat          text eol=crlf
*.cmd          text eol=crlf

# 4. Binary Files:
# Tell Git these are not text files. Git will not attempt to normalize them.
*.png          binary
*.jpg          binary
*.jpeg         binary
*.gif          binary
*.webp         binary
*.ico          binary
*.jar          binary
*.zip          binary
*.aar          binary
*.so           binary
*.pdf          binary
*.ttf          binary
*.otf          binary
